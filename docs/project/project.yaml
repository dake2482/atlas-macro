schema_version: 1
project_id: AI-RESEARCH
name: Atlas Macro
purpose: Build a clean-room macro-research and AI supply-chain intelligence platform with traceable public sources, explicit data-quality labels, and original presentation.
success_metrics:
  - Public research outputs preserve source lineage, observation time, quality state, and licence boundaries.
  - Demo or fallback records are never presented as verified official data.
  - Django checks, lint, and deterministic tests remain green.
current_milestone:
  id: AI-RESEARCH-021
  title: 联储 H.10 外汇参考序列严格公开合同
  status: REVIEW
  evidence:
    - AI-RESEARCH-020 is in REVIEW after 771 full-suite tests, live six-source composition, HTTP 200 route smoke and final P0=0/P1=0 Sol review; its 1440/390 Browser Plugin gate remains explicitly unexecuted.
    - Current `/assets/fx/` live truth returns HTTP 200 from snapshot 111, but that snapshot is unversioned, stale as of 2026-07-02 and contains four metrics with no versioned charts or sections.
    - The current registry still embeds prototype DXY, USD/CNH and offshore-funding language although no ICE DXY, CNH, forwards/NDF or cross-currency-basis source is licensed.
    - The latest local Federal Reserve H.10 run is successful with 37,303 normalized observations and one immutable private ZIP artifact, so source acquisition already exists and the remaining risk is weak generic publication.
    - AI-RESEARCH-021 now publishes an independent H.10 v1 contract with four official reference metrics, two exact historical charts and three rendered observation/method/licensing-gap sections.
    - Assets FX reuses the audited neutral H.10 run/artifact/Observation validator from AI-RESEARCH-019 without depending on or copying the global-dollar parent snapshot; both pages retain separate publication hashes, batches and MetricSnapshot rows.
    - ICE DXY, executable institutional spot, CNH, forwards/NDF, cross-currency basis and dealer/order-book data remain explicit PURCHASE_REQUIRED or LICENSE_REVIEW gaps until public-derived-display and storage rights are obtained.
    - A clean temporary SQLite database fetched the live Federal Reserve H.10 archive in 23.68 seconds, stored 37,323 exact observations and one 2,072,667-byte private artifact, published one strict v1 snapshot with four MetricSnapshots, and returned HTTP 200 for `/assets/fx/` and `/assets/`.
    - Final validation passed 790 tests, Ruff, Django system checks, migration drift and `git diff --check`; the focused Sol review reported P0=0, P1=0 and P2=0.
    - The 1440/390 Browser Plugin visual and console gate remains explicitly unexecuted because the supported plugin runtime regression is still unresolved.
health: YELLOW
next_action: Run the deferred 1440/390 Browser Plugin visual and console gate after its supported runtime regression is fixed; production deployment remains separately authorized.
updated_at: '2026-07-15T11:56:04+08:00'
validation_commands:
  - .venv/bin/ruff check .
  - .venv/bin/pytest -q
  - .venv/bin/python manage.py check
