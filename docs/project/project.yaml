schema_version: 1
project_id: AI-RESEARCH
name: Atlas Macro
purpose: Build a clean-room macro-research and AI supply-chain intelligence platform with traceable public sources, explicit data-quality labels, and original presentation.
success_metrics:
  - Public research outputs preserve source lineage, observation time, quality state, and licence boundaries.
  - Demo or fallback records are never presented as verified official data.
  - Django checks, lint, and deterministic tests remain green.
current_milestone:
  id: AI-RESEARCH-017
  title: 流动性次表层资金压力精确多源公开合同
  status: REVIEW
  evidence:
    - AI-RESEARCH-017 now uses a dedicated exact-batch coordinator and strict public selector with 11 metrics, four charts, two rendered tables and fail-loud main/PRATES refresh wiring; the generic publisher bypass was removed.
    - SOFR, SRF and USD swaps retain exact NY Fed JSON bytes in private content-addressed artifacts, while PRATES retains its ZIP hash. Small-value technical exercises are partitioned and independently reconciled for SRF and swaps.
    - Independent adversarial review closed four P1 publication bypasses and one normalized-evidence P2. Repository-wide Ruff, Django check, migration drift, diff check and all 674 deterministic tests pass.
    - A clean temporary database live-refresh published the subsurface page from four successful official runs with exactly one immutable artifact each; HTTP route smoke returned 200 and rendered 20 common-date rows plus 40 operation rows.
    - The comparison site's opaque composite pressure score remains NEEDS_SOURCE and dealer-level microstructure remains PURCHASE_REQUIRED rather than being inferred.
health: YELLOW
next_action: Re-run the pending 1440/390 browser acceptance when the Browser Plugin runtime is fixed. Production deployment remains separately authorized.
updated_at: '2026-07-14T14:18:51+08:00'
validation_commands:
  - .venv/bin/ruff check .
  - .venv/bin/pytest -q
  - .venv/bin/python manage.py check
